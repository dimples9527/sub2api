# 仓库协作规则

## 文档语言

- 后续新增或修改的文档必须使用中文书写。

## 框架与非框架代码边界

- 本项目约定：模型监控模块、供应商管理模块以及模型广场模块属于非框架业务代码；除上述模块外的代码默认属于框架本身。
- 模型监控相关的后端路由、处理器、服务、仓储、测试，以及 `frontend/src/views/admin/model-monitor`、模型监控 API 和模型监控静态页面，均按非框架业务代码管理。
- 供应商管理相关的后端路由、处理器、服务、仓储、测试，以及 `frontend/src/views/admin/supplier-management`、供应商管理 API，均按非框架业务代码管理。
- 模型广场相关的后端处理器、路由、测试，以及模型广场前端组件、视图、API 和测试，均按非框架业务代码管理；其中管理端模型广场当前位于 `frontend/src/views/admin/upstream-management/ModelSquareView.vue`，不得仅因目录名称将其误判为框架代码。
- 新增功能应优先放入对应的非框架业务模块，避免把业务规则、业务数据、页面和模块专用组件写入框架代码。只有在现有程序没有扩展点时，才允许保留最小的框架接入代码，并在改动说明中明确接入原因和范围。
- 不得把非框架模块的业务逻辑泛化为全局框架能力；不得为了接入一个非框架模块而修改原有框架数据表、核心枚举或无关模块。
- 复查改动时，必须逐项区分“非框架业务代码”和“框架接入代码”，并优先评估是否可以通过模块内路由、服务、仓储、页面或既有配置入口迁移出框架。

## 前端组件使用

- 前端页面改动只能使用当前项目已有组件；不得为满足页面需求而修改、优化或扩展现有通用组件的内容。
- 如果现有组件能力不足，应在页面层通过组合、适配或显式数据转换解决；确需调整组件本身时，必须先征得用户明确同意。
- 涉及页面样式、配色、布局、动效或任何视觉层级调整时，必须先使用 `frontend-design` 技能，再开始修改代码。

## Wire 依赖注入

- 新增或调整单一业务模块的 Wire provider 时，应优先放在对应 package 的 `<module>_wiring.go` 专用文件中；公共 `wire.go` 只保留 `ProviderSet` 的最小注册项。
- `wire_gen.go` 是 Wire 生成文件，不得手工编辑、拆分或迁移其中内容。依赖图变化后必须重新运行 Wire，并保留必要的生成结果变更。
- 不得为了让 `wire.go` 或 `wire_gen.go` 保持零差异而绕过依赖注入、重复仓储逻辑或引入隐式全局依赖。

## 提交信息

- 未经用户明确要求，不主动执行 `git commit`；只有在用户明确要求提交时才执行提交。
- 提交信息必须使用类型前缀，例如 `feat:`、`fix:`、`style:`、`refactor:`、`test:`、`docs:`、`chore:`、`build:`。
- 冒号后的描述必须使用中文。
- 描述要简洁、具体，只概括本次提交的变更。

示例：

- `feat: 新增上游账号删除操作`
- `fix: 修复上游同步日志刷新后丢失`
- `style: 固定上游账号表格列宽`

## 供应商模块搜索控件

- 供应商管理、账号管理、分组管理等页面的搜索与筛选区域，优先复用项目现有的 `Input`、`Select`、`SearchInput` 组件，并在页面层组合布局。
- 保留页面已有的统一筛选卡片作为外层容器；`Input`、`Select` 等控件外层只使用宽度类（如 `w-full`、`w-40`），不得把带边框、背景和内边距的 `sp-search` 样式同时挂到组件外层，避免产生紧贴控件的双层边框。
- 搜索控件上方不显示额外字段小标题；确需提供无障碍名称时，使用 `sr-only` 隐藏文本保留 `aria-labelledby` 关联。

## 前端提示反馈

- 页面保存、校验失败、接口成功/失败等操作反馈，必须使用框架统一 Toast：通过 `useAppStore()` 调用 `showSuccess` / `showError`（全局 `Toast` 组件已在 `App.vue` 挂载）。
- 禁止在页面内自行实现 `sp-toast`、`sp-alert` 临时提示条、本地 `toast`/`error` ref + `setTimeout` 等自定义提示方案。
- 业务错误文案优先复用 `extractApiErrorMessage`（或页面已有的等价错误提取方法）后再交给 `showError`，保证提示风格与其他管理页一致。

## 供应商模块弹窗内容

- 使用 `BaseDialog` 等已经提供标题的弹窗时，弹窗内容区左上角不得重复显示英文眉题、二级标题或说明文案；只有完成当前操作不可缺少的业务提示才能保留。
- 列表型弹窗应优先直接展示筛选、状态摘要或列表内容，不得在列表上方增加与弹窗标题重复的装饰性标题和说明段落。

## 供应商模块弹窗样式与 Teleport

- `BaseDialog` 会通过 `Teleport` 挂载到 `body`，弹窗内容会脱离 `.supplier-management-page` 祖先节点。
- 供应商主题 CSS 变量（如 `--sp-panel`、`--sp-line`、`--sp-green`、`--sp-blue`、`--sp-cyan`、`--sp-violet` 等）定义在 `.supplier-management-page` 上；Teleport 后这些变量默认不可用，依赖 `var(--sp-*)` / `color-mix(... var(--sp-*) ...)` 的配色会失效，表现为“改了颜色但弹窗里看不出来”。
- 在 `BaseDialog` 内做配色、状态色、KPI 卡、标签等样式时，必须任选一种可靠方案，禁止只依赖页面根节点变量：
  1. 在弹窗内容根节点自行声明完整的 `--sp-*` 兜底变量（推荐用于页面 scoped 样式）；
  2. 或像 `SupplierDrawer` / `SupplierModal` 一样，给 Teleport 后的内容根节点挂上 `supplier-management-page`（若挂该类，需注意并覆盖其页面级 `min-height` 等布局副作用）；
  3. 对关键强调色可同时使用明确色值兜底，避免变量缺失时完全无色。
- 验收时必须打开真实弹窗确认颜色生效；不能只看页面内非 Teleport 区域，也不能只靠代码里写了 `var(--sp-*)` 就视为完成。
