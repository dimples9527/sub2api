# 供应商运维驾驶舱真实数据接入交接

## 任务目标

将 `frontend/src/views/admin/supplier-management/SupplierDashboardView.vue` 从静态演示数据切换为真实后台只读数据，支持 `24h`、`7d`、刷新、局部加载/错误/空状态和真实下钻。

## 必须继续遵守的约束

1. **不得修改、创建或删除任何文件名以 `upstream_*` 开头的文件。**
2. 可参考 upstream 模块代码，但新实现必须写入 `supplier_dashboard_*` 等新文件。
3. 不修改前端通用组件；页面层通过组合和显式数据转换解决。
4. 页面改动前使用 `frontend-design` 技能。
5. 严格 TDD：先确认 RED，再实现 GREEN。
6. 每个任务依次经过规格审查和代码质量审查，问题修复后必须复审。
7. `wire_gen.go` 只能通过 Wire 生成，禁止手工编辑。
8. 工作区存在其他任务的未提交改动，禁止修改、恢复或提交这些无关文件。

## 本次已完成内容

### Repository

新增：

```text
backend/internal/repository/supplier_dashboard_repo.go
backend/internal/repository/supplier_dashboard_repo_test.go
backend/internal/repository/supplier_dashboard_repo_integration_test.go
backend/internal/repository/supplier_dashboard_wiring.go
```

已实现异常账号、Provider + Group 倍率分析、供应商运行概览三类只读快照，主要规则包括：

- 时间窗口统一为 `[start,end)`。
- `usage_logs` 每条记录视为成功请求，包括零成本请求。
- `ops_error_logs.status_code >= 400` 为失败请求，并排除 `is_count_tokens=true`。
- 本地账号和供应商账号采用双向唯一匹配；同供应商或跨供应商冲突均不归属流量。
- 匹配但无流量返回真实 `0`；未匹配或冲突返回 `null`。
- 倍率变化读取 migration 187 的 `supplier_rate_guard_change_logs`。
- accounts、groups、balance、all 同步证据独立处理。
- Provider、Group 条件已安全下推，同时保留跨 Provider 冲突检测。
- SQL 完整处理 `QueryContext`、`Scan`、`rows.Close()` 和 `rows.Err()`。

### Service

新增：

```text
backend/internal/service/supplier_dashboard.go
backend/internal/service/supplier_dashboard_test.go
backend/internal/service/supplier_dashboard_wiring.go
```

已实现：

- `24h`、`7d` 查询范围。
- Accounts、Rates、Providers 查询、筛选、排序和安全分页。
- `null` 与真实零值区分。
- `int64` 溢出保护、NaN/Inf 降级和 context 错误传播。
- Provider 状态按已知最高严重度计算。
- `critical_issue_count` 可空。
- 余额风险仅依赖独立 balance sync 成功证据。
- Account `DetectedAt` 使用实际命中风险的证据时间。

### 查询支持迁移

新增：

```text
backend/migrations/192_supplier_dashboard_query_schema.sql
backend/migrations/193_supplier_dashboard_query_indexes_notx.sql
```

Migration 192 包含：

- `supplier_provider_accounts.supplier_dashboard_normalized_effective_name` 持久化规范化账号键。
- 账号名称、Provider 前缀、active、Provider 软删除/恢复变化时的触发器维护和既有数据回填。
- `supplier_provider_runtime_stats.risk_updated_at` 风险专属时间。
- 仅在风险等级或风险依据变化时更新时间；余额、费用、同步等无关更新不改变该时间。

Migration 193 使用 `_notx.sql` 和 `CREATE INDEX CONCURRENTLY IF NOT EXISTS`，为本地账号规范化、供应商账号规范化、Provider + Group、健康任务、同步运行和倍率变化查询增加索引。

### Wire 最小注册

已修改：

```text
backend/internal/repository/wire.go
backend/internal/service/wire.go
```

仅增加 `SupplierDashboardWiringSet` 最小注册。当前尚无生产 Handler 消费新 Service，因此 Wire 会剪枝该依赖，`wire_gen.go` 暂无本模块实例化代码。

## 最近验证结果

实现者最近报告以下命令通过，使用 `GOCACHE=D:\tmp\sub2api-go-build`：

```text
go test ./internal/repository -run SupplierDashboard -count=1
go test -tags=integration ./internal/repository -run SupplierDashboard -count=1
go test ./internal/service -run SupplierDashboard -count=1
go test ./internal/repository -run Migration -count=1
go test ./migrations -count=1
go vet ./internal/repository ./internal/service ./migrations
```

Integration 使用 Testcontainers PostgreSQL 18.1，未 skip。另已验证格式、空白检查和 `upstream_*` 零差异；默认 PostgreSQL planner 在大基数测试数据中使用供应商账号和倍率历史目标索引。

## 审查状态

Repository/Service 阶段此前已完成多轮规格和代码质量审查。最后一轮质量审查提出：供应商账号侧全局规范化、倍率历史全局扫描、普通事务内建索引风险、Provider critical 使用通用 runtime 更新时间，以及默认 planner 测试不足。

实现者已完成上述最新修复并通过定向验证，但**本次会话在最终复审前结束，最新修复尚未再次经过独立规格复审和代码质量复审**。

下次继续时必须先完成这两轮复审；未通过前不要进入 Handler 任务。

## 本轮继续结果（2026-07-28）

### 1. Repository/Service 复审

规格复审：通过

- 持久化规范化键 supplier_dashboard_normalized_effective_name 仍配合全局 reverse candidate，不破坏同 Provider / 跨 Provider 双向唯一匹配。
- 触发器覆盖账号 provider_id/name/active 与 Provider account_name_prefix/deleted_at；integration 已覆盖前缀、软删恢复、active 与回填。
- 倍率历史通过 target provider/group -> mapping -> history.mapping_id 访问，并保留事件自身 upstream_group_key 语义。
- risk_updated_at 仅随 risk_level / rate_risk_count 变化；无关 runtime 更新保持稳定。
- 192 事务 schema 与 193 _notx.sql CONCURRENTLY 索引按 runner 规则分离。
- DTO、[start,end) 时间窗、分页、null/零值、Provider 状态语义无回归。

质量复审：通过

- 单元/迁移测试与 vet 通过：go test ./internal/repository -run SupplierDashboard|Migration、./internal/service -run SupplierDashboard、./migrations。
- 默认 planner 与 filtered EXPLAIN integration 覆盖目标索引；non-transactional concurrent index 安全。
- 非阻塞备注：Provider 前缀变更触发器会批量改写该 Provider 下账号规范化键（低频运维操作）。

### 2. Handler / Wire / 路由

已完成：

```text
backend/internal/handler/admin/supplier_dashboard_handler.go
backend/internal/handler/admin/supplier_dashboard_handler_test.go
backend/internal/handler/admin/supplier_dashboard_wiring.go
backend/internal/server/routes/supplier_dashboard.go
```

并最小改动：

- backend/internal/handler/handler.go 增加 SupplierDashboard
- backend/internal/handler/wire.go 注册 admin.SupplierDashboardWiringSet 并注入 ProvideAdminHandlers
- backend/internal/server/routes/admin.go 调用 registerSupplierDashboardRoutes
- backend/cmd/server/wire_gen.go 由 wire 生成，已实例化 Repository/Service/Handler

接口：

```http
GET /api/v1/admin/upstream-management/dashboard/accounts
GET /api/v1/admin/upstream-management/dashboard/rates
GET /api/v1/admin/upstream-management/dashboard/providers
```

默认：range=24h、risk_type=all、view=risk、page=1、page_size=20；非法枚举/分页返回 400；业务错误走 response.ErrorFrom。

验证：

```text
go test ./internal/handler/admin -run SupplierDashboard -count=1
go test ./internal/service -run SupplierDashboard -count=1
go test ./cmd/server -run '^$' -count=1
upstream_* 相关保护文件 zero diff
```

### 3. 前端 API

已完成：

```text
frontend/src/api/admin/supplierDashboard.ts
frontend/src/api/admin/supplierDashboard.spec.ts
```

- overview 继续请求旧 `/dashboard`
- 新增 accounts / rates / providers
- DTO 映射 snake_case 与 nullable 字段
- 全部支持 AbortSignal；默认 range=24h
- API spec：3 tests 通过

### 4. 页面接入真实数据（2026-07-28 收尾）

已完成：

```text
frontend/src/views/admin/supplier-management/SupplierDashboardView.vue
frontend/src/views/admin/supplier-management/SupplierDashboardView.spec.ts
```

实现要点：

- 删除静态演示数组；无 gemini-backup / 执行账号巡检 等演示动作
- 并行四路：getOverview + getAccounts + getRates + getProviders
- AbortController + 全局 requestSeq；accounts / rates 另有局部 seq 防竞态
- 范围切换 / 刷新 → 全量；风险筛选 → 只拉 accounts；倍率 Tab → 只拉 rates
- 分区独立 loading / data / error / lastUpdated；空数据不回退演示值
- 下钻仅 router.push(target_path | settings_path)；主按钮为「查看自动任务」
- null 显示为 —，真实 0 显示 0
- 默认 selectedRisk=critical、rateView=risk、range=24h
- 页面层 CSS：sp-rate-delta → sp-rate-cell，sp-provider-top → sp-provider-head（复用共享样式，不改通用组件）

页面验证（2026-07-28）：

```text
pnpm.cmd test:run src/api/admin/supplierDashboard.spec.ts src/views/admin/supplier-management/SupplierDashboardView.spec.ts
→ 13 passed (API 3 + View 10)
pnpm.cmd typecheck → exit 0
eslint 驾驶舱相关 4 文件 → exit 0
upstream_* 保护文件 zero diff
```

### 5. 当前状态与剩余事项

- 真实只读数据接入主线已完成（Repo / Service / Migration / Handler / Wire / 前端 API / 页面）。
- 未经用户明确要求，不要主动 commit。
- 提交时仅 stage 本任务文件；排除 SupplierAutomationView、supplierAutomation.ts、health_guard、rate_guard、migration 191、findings/progress/task_plan 等无关改动。
- 可选后续：整仓 pnpm lint:check / build；联调真实后端数据观感。
## 下一步实施顺序（历史，已完成）

原计划顺序 1–4（复审 → Handler/Wire → 前端 API → 页面接入）均已完成，详见上方「本轮继续结果」。
后续若提交，按「工作区无关改动」排除清单与保护检查执行。
## 建议验证命令

后端：

```powershell
Set-Location backend
$env:GOCACHE='D:\tmp\sub2api-go-build'
go test ./internal/repository -run SupplierDashboard -count=1
go test -tags=integration ./internal/repository -run SupplierDashboard -count=1
go test ./internal/service ./internal/handler/admin -run SupplierDashboard -count=1
go test ./internal/repository -run Migration -count=1
go test ./migrations -count=1
go generate ./cmd/server
go test ./cmd/server -run '^$' -count=1
go vet ./internal/repository ./internal/service ./internal/handler/admin ./migrations ./cmd/server
```

前端：

```powershell
Set-Location frontend
pnpm.cmd test:run src/api/admin/supplierDashboard.spec.ts
pnpm.cmd test:run src/views/admin/supplier-management/SupplierDashboardView.spec.ts
pnpm.cmd typecheck
pnpm.cmd lint:check
pnpm.cmd build
```

保护检查：

```powershell
git status --short | Select-String 'upstream_'
git diff --exit-code -- backend/internal/service/upstream_dashboard.go backend/internal/service/upstream_dashboard_test.go backend/internal/handler/admin/upstream_dashboard_handler.go backend/internal/handler/admin/upstream_dashboard_handler_test.go backend/internal/server/routes/upstream_management.go
```

## 工作区无关改动

本次提交不得包含：

```text
backend/internal/repository/supplier_provider_data_repo.go
backend/internal/repository/supplier_provider_data_repo_test.go
backend/internal/service/supplier_account_health_guard.go
backend/internal/service/supplier_account_health_guard_test.go
backend/internal/service/supplier_account_rate_guard.go
backend/internal/service/supplier_account_rate_guard_test.go
backend/internal/service/supplier_automation_service.go
backend/internal/service/supplier_automation_service_test.go
backend/internal/service/supplier_group_guard_test.go
frontend/src/api/admin/supplierAutomation.ts
backend/migrations/191_supplier_account_health_guard_whitelist.sql
findings.md
progress.md
task_plan.md
```

公共 `backend/internal/repository/wire.go` 和 `backend/internal/service/wire.go` 仅提交 Supplier Dashboard 单行注册。
